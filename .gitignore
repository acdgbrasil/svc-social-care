# SwiftPM
.build/
.swiftpm/
Package.pins

# Local environment
.env
.env.*
!.env.example

# Logs and OS
*.log
.DS_Store
.vscode/

# Temp
.cache/
.tmp/

# Claude Code — settings.json é compartilhado; o .local.json é por máquina
# (permissões com paths absolutos do dev) e não deve ser versionado.
.claude/settings.local.json
